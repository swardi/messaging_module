  input NoteInput {
      noteText: String
      timeStamp:   String
      author : String
  }

  type Note {
      id: ID!
      noteText: String!
      author: String!
      timeStamp : String!
   }

  type Query {
      getNote(id: ID!): Note
      getNotes : [Note]
      author : String!
  }

  type Mutation {
      createNote(input: NoteInput): Note
      updateNote(id: ID!, input: NoteInput): Note
      deleteNote (id:ID!): Note
  }
